<%= render 'shared/page_title', title: @recipe.name %>

<div class="col-md-6 offset-3">

  <h4 class="description"><storng>Steps: </storng></h4>
  <hr />
  <%= simple_format(@recipe.description) %>
  <hr />
  <div class="text-right chef">
    <p class="center">
      <div class="text">
        Crerated by:
      </div>
    </p>
    <p class="center">
      <%= link_to gravatar_for(@recipe.chef), recipes_path %>
    </p>
    <p class="center">
      <%= @recipe.chef.chefname.capitalize %>
      <div class="text-right"><%= time_ago_in_words(@recipe.created_at) %> ago</div>
    </p>
  </div>
  <div class="recipe-action">
    <%= link_to "Edit this recipe", edit_recipe_path(@recipe), class: "btn btn-success btn-sm" %>
    <%= link_to "Delete this recipe", recipe_path(@recipe), method: :delete,
                                      data: { confirm: "Are you sure?" },
                                      class: "btn btn-danger btn-sm" %>
    <%= link_to "Return to recipes listing", recipes_path, class: "btn btn-primary btn-sm" %>
  </div>
</div>
